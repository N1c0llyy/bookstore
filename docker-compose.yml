services:
  web:
    build: .
    container_name: bookstore_app
    command: python bookstore/manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    networks:
      - bookstore-network

networks:
  bookstore-network:
    driver: bridge